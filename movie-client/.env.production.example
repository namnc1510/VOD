# --- FRONTEND PRODUCTION CONFIGURATION ---
# Replace the values below with your actual production backend URL

# The URL of your deployed backend (e.g. from Render)
# Must end with /api/v1
VITE_API_BASE_URL=https://movie-backend-api.onrender.com/api/v1

# Timeout for API requests in milliseconds
VITE_API_TIMEOUT_MS=60000
